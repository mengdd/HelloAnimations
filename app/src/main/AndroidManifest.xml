<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ddmeng.helloanimations">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".property.BasicPropertyAnimationDemo"
            android:label="@string/basic_property_animation_demo" />
        <activity
            android:name=".property.ViewPropertyAnimationDemoActivity"
            android:label="@string/view_property_animation_demo" />
        <activity android:name=".property.KeyframeDemoActivity"></activity>
    </application>

</manifest>